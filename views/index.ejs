<%- include header %>
<% posts.forEach(function (post, index) { %>
	<p><h2><a href="#"><%= post.title %></a></h2></p>
	<p class="info">
		Tác giả: <a href="#"><%= post.name %></a> |
		Ngày: <%= post.time.minute %>
	</p>
	<p><%- post.post %></p>
<% }) %>
<%- include footer %>